data/

# Compiled Go binaries (platform-specific, never commit)
/agw
/gateway
/service
/gdpr-delete
/tool_register
/s3-mcp-server
/spike-cli
/openclaw-ws-smoke
/mtls-healthcheck
/tcp-healthcheck
/spike-seeder
/messaging-sim
/ws-e2e-client
/bin/
demo/go/demo
demo/mock-mcp-server/mock-mcp-server
*.exe
*.out
*.test

# Build artifacts (entire directory is ephemeral)
build/
coverage.out

# Generated reports
reports/

# Attestation signing key (NEVER commit private keys)
config/attestation-ed25519.key

# Upgrade workflow artifacts (runtime only, never commit)
config/versions.yaml.snapshot.*
config/upgrade-snapshots/

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
uv.lock
.env
.env.local
.env.*.local

# Repave workflow state (runtime artifact)
.repave-state.json

# OpenTofu / Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
!*.tfvars.example
.terraform.lock.hcl

# E2E test artifacts (runtime only)
tests/e2e/artifacts/
tests/e2e/*.log

# Relocated use-case-specific files (local reference only)
use_case_specific/

# Generated operational docs and snapshots
docs/operations/artifacts/
docs/upgrades/
make-*.log

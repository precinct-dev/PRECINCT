# NetworkPolicies: default-deny + explicit allow for gateway and tools namespaces
# Cloud-agnostic -- works on any cluster with a NetworkPolicy-capable CNI
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - default-deny.yaml
  - gateway-allow.yaml
  - mcp-server-allow.yaml

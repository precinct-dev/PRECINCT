# Kustomize resource group for gateway manifests
# Added by RFA-9fv.6 to support Kustomize overlay references
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - gateway-namespace.yaml
  - gateway-rbac.yaml
  - gateway-configmap.yaml
  - gateway-deployment.yaml
  - gateway-service.yaml

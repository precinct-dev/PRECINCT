apiVersion: v1
kind: Secret
metadata:
  name: gateway-runtime-secrets
  namespace: gateway
type: Opaque
stringData:
  # Replace in real production with a secret-manager managed value.
  approval_signing_key: "prod-approval-signing-key-material-at-least-32"

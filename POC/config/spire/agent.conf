agent {
    data_dir = "/opt/spire/data/agent"
    log_level = "DEBUG"
    server_address = "spire-server"
    server_port = "8081"
    socket_path = "/tmp/spire-agent/public/api.sock"
    trust_bundle_path = "/opt/spire/conf/agent/bootstrap.crt"
    trust_domain = "agentic-security.local"

    # Insecure bootstrap for POC only
    insecure_bootstrap = true
}

plugins {
    NodeAttestor "join_token" {
        plugin_data {}
    }

    KeyManager "disk" {
        plugin_data {
            directory = "/opt/spire/data/agent"
        }
    }

    # Docker workload attestor - identifies workloads by container properties
    WorkloadAttestor "docker" {
        plugin_data {
            # Docker daemon socket - must be mounted from host
            docker_socket_path = "/var/run/docker.sock"

            # Container label used for workload identity
            container_id_cgroup_matchers = [
                "/docker/*"
            ]
        }
    }

    # Unix workload attestor for processes
    WorkloadAttestor "unix" {
        plugin_data {}
    }
}

health_checks {
    listener_enabled = true
    bind_address = "0.0.0.0"
    bind_port = "8080"
    live_path = "/live"
    ready_path = "/ready"
}

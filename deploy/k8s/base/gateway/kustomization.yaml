# Kustomize resource group for gateway manifests (cloud-agnostic)
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - gateway-namespace.yaml
  - gateway-rbac.yaml
  - gateway-configmap.yaml
  - gateway-deployment.yaml
  - gateway-service.yaml

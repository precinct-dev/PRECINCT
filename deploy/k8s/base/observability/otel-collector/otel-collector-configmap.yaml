# ------------------------------------------------------------------------------
# OpenTelemetry Collector ConfigMap
# Cloud-agnostic Kubernetes manifest -- works on any conformant cluster.
#
# Collector configuration for in-cluster telemetry pipeline.
# Key points:
#   - Phoenix endpoint uses Kubernetes service DNS name
#   - Resource attributes include k8s namespace info
#   - Health check extension for liveness/readiness probes
#
# Pipeline:
#   receivers (otlp gRPC+HTTP) -> processors (batch, resource) -> exporters (Phoenix)
# ------------------------------------------------------------------------------
apiVersion: v1
kind: ConfigMap
metadata:
  name: otel-collector-config
  namespace: observability
  labels:
    app.kubernetes.io/name: otel-collector
    app.kubernetes.io/part-of: mcp-observability
    app.kubernetes.io/component: collector
data:
  otel-collector-config.yaml: |
    extensions:
      health_check:
        endpoint: 0.0.0.0:13133

    receivers:
      otlp:
        protocols:
          grpc:
            endpoint: 0.0.0.0:4317
          http:
            endpoint: 0.0.0.0:4318

    processors:
      batch:
        timeout: 1s
        send_batch_size: 1024

      # Add resource attributes for observability context
      resource:
        attributes:
          - key: service.namespace
            value: agentic-security-poc
            action: upsert
          - key: deployment.environment
            value: k8s-poc
            action: upsert

    exporters:
      # Export to Phoenix for trace visualization via OTLP gRPC
      otlp/phoenix:
        endpoint: phoenix.observability.svc.cluster.local:4317
        tls:
          insecure: true

      # Debug logging (set to basic in production)
      debug:
        verbosity: basic

    service:
      extensions: [health_check]
      pipelines:
        traces:
          receivers: [otlp]
          processors: [batch, resource]
          exporters: [otlp/phoenix, debug]

        metrics:
          receivers: [otlp]
          processors: [batch, resource]
          exporters: [debug]

        logs:
          receivers: [otlp]
          processors: [batch, resource]
          exporters: [debug]

      telemetry:
        logs:
          level: info

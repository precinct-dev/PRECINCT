apiVersion: v1
kind: Service
metadata:
  name: mock-guard-model
  namespace: gateway
  labels:
    app.kubernetes.io/name: mock-guard-model
    app.kubernetes.io/component: guard-model-alias
spec:
  type: ExternalName
  externalName: mock-guard-model.tools.svc.cluster.local
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
